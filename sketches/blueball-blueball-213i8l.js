let x, y; let speedX, speedY; let ballSize = 50; function setup() { createCanvas(800, 600); x = width / 2; y = height / 2; speedX = random(-5, 5); speedY = random(-5, 5); } function draw() { background(44, 62, 80); // Update position x += speedX; y += speedY; // Bounce off edges if (x > width - ballSize / 2 || x < ballSize / 2) { speedX *= -1; } if (y > height - ballSize / 2 || y < ballSize / 2) { speedY *= -1; } // Randomly change direction slightly if (frameCount % 60 === 0) { speedX += random(-1, 1); speedY += random(-1, 1); // Limit max speed speedX = constrain(speedX, -8, 8); speedY = constrain(speedY, -8, 8); } // Draw ball fill(52, 152, 219); noStroke(); ellipse(x, y, ballSize, ballSize); // Draw shadow fill(0, 0, 0, 50); ellipse(x + 5, y + 5, ballSize, ballSize); }